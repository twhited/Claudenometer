services:
  chronoconnect:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      TRANSPORT: sse
      HOST: "0.0.0.0"
      PORT: "8000"
    restart: unless-stopped
